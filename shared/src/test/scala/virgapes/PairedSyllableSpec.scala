package edu.holycross.shot.virgapes
import org.scalatest.FlatSpec



/**
*/
class PairedSyllableSpec extends FlatSpec {

  "A PairedSyllable" should "require equal numbers of syllables" in pending /* {
    try {
      val txtSylls = "pu-er"
      val nm = Neume(1,1,0,0)
      val nmSylls  = Syllable(Vector(nm))
      PairedSyllable(txtSylls, nmSylls)
      fail("Should have rejected mismatched number of syllables")
    } catch {
      case iae: IllegalArgumentException => {
        assert(iae.toString.contains("PairedSyllable: unequal number of syllables"))
      }
      case t: Throwable => fail("Should have thrown assertion error, but threw " + t)
    }
  }*/
}
